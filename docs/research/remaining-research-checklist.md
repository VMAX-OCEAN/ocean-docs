# Remaining Research Checklist

Date: 2026-09-10. Authority: SIH26067. Rule: no gate pass = no milestone pass. Check box only with measured evidence + access date, never estimates.

## D1 — Dataset cards (blocks M2) — all 4 written 2026-09-10

- [x] `docs/data/glorys12.md` — DOI corrected to `10.48670/moi-00021` (brief's `moi-00019` = BGC 001_029). Copernicus licence verbatim, bbox -180/180 -90/90, grid 4320×2041 1/12°, temporal 1993-01-01→M-1 (daily obs to 23/06/2026, monthly to 01/05/2026), 4 3D + 7 2D + 8 static vars exact abbrev/standard_name/unit, 3 dataset IDs. sha NONE metadata-only, ingest v0. PUM issue 1.7 Nov 2025 28pp. Error found: `glorys-dataset.md:46` lists `wmo` vertical velocity — does not exist in 001_030, only uo/vo. Flagged in card.
- [x] `docs/data/argo.md` — DOI `10.17882/42182` CC-BY 4.0 verbatim. FTP + HTTPS + s3/rsync/ERDDAP. 2 sample sha256 (bulk NONE mutable tree): `6901254_prof.nc` `b6ff7615…` 1199768 B, `BD1900722_001.nc` `ee5d3c83…` 22880 B. Vars units/standard_name/QC 0-9 read direct from NetCDF headers. Indian subset 354,696 files/2,421 floats. 931 GB claim UNVERIFIED (2023-era); live count 3,386,798 records/20,484 floats. Bug found in `argo-data.md:85-87`: `region([-90,30,180,90,dates])` wrong argopy order, depth pair required — noted not fixed.
- [x] `docs/data/glider.md` (306 lines) — FTP live 185 entries; HTTPS tree mirror DEAD (000); alt SEANOE DOI `10.17882/56509` + ERDDAP. Sample sha256 `71976eee…` 2,464,969 B. License CC-BY-NC 4.0 verbatim (NC — commercial check). Vars TEMP/PSAL/PRES/CNDC/DOXY + EGO QC table. Indian Ocean verdict: present but sparse+clustered — 131/1,115 deployments (11.7%), 63,153/824,632 profiles (7.7%), 124/131 = single Mayotte REVOSIMA array. Availability RISK not gap. All 824,632 profiles real-time only (0 delayed/provisional). ERDDAP tabledap HTTP 400 via proxy. Error found: `glider-data.md` trailing `D` = descending profile NOT delayed-mode (EGO manual §6.2). Noted not fixed.
- [x] `docs/data/insitu-collection.md` (9.8K) — blank is AUTHORITY-SIDE. Authority `https://sih.gov.in/sih2026PS` HTTP 200 modal `#ViewProblemStatement26067` item d `Collection of In-situ Data:` ends at colon no URL, blank 3x in HTML + mirror JSON. PS opens "links are missing and should be included" = placeholder. Mirror attachment = `SIH26066.pdf` wrong PS. 8 candidates ranked: C1 INCOIS holdings (in incois-las.md), C2 OON, C3 OMNI-RAMA (uncovered), C4 ERDDAP floats, C5 ESSDP, C6 NOAA GDP, C7 Copernicus in-situ (unsupported), C8 Argo/Glider re-list excluded. Action: ask SIH/INCOIS for item-d URL via portal contact + MoES nodal officer, quote PS ID. Fail closed. No invented URL.

Fixture-use: all 4 now card-complete for metadata; sha present only where a sample was hashed (argo, glider); glorys12 + incois-las metadata-only (live APIs). M2 gate can reference cards; still needs ingest v1 + measured runs.

## D2 — Reference laptop + measured runs (blocks M2/M4) — protocols written 2026-09-10, runs still open

- [x] Reference laptop locked: **Acer Aspire 7 A715-79G** (Core 7 240H, RTX 3050 6 GB, 16 GB dual, 144 Hz). 144 Hz required — 60 Hz panel caps FPS measurement. iGPU configs (Iris Xe A, Radeon B) kept as compatibility floor. WebGL2 HARD requirement on cesium 1.142 (billboards/labels + pickAsync); WebGPU irrelevant (no Cesium impl). GPU check snippet + 17 record fields + browser matrix (Chrome 153 / Edge 152 / Firefox 155 / ESR 140). File: `docs/performance/reference-laptop.md`.
- [x] Chunk benchmark protocol written + harness: exact subset build (copernicusmarine 2.4.1), both chunkings Zarr v2 (`zarr_format=2`), on-disk chunk stats, R2 upload, CORS/Range probe, browser harness (zarrita 0.7.5 FetchStore), R2 dashboard Class A/B, gates G1–G9, decision dual-rep vs balanced. Effective balanced chunk `(1,10,480,420)`. File: `docs/performance/chunk-benchmark-protocol.md`. RUN open.
- [x] Render protocol written + harness: slice p95 ≤750 ms, cold ≤10 s, 30 FPS + 5000 markers (entity vs PointPrimitive arms), terrain bytes, ion 20-reload headroom, airplane-mode. Notes BLOCKED: no slice UI / no Zarr provider mounted / zero marker code in `src/` — instrumentation hooks defined. File: `docs/performance/benchmark-protocol.md` + `ocean-web/scripts/bench-harness.js` (selftest PASS). RUN open.
- [x] Marker cutoff protocol written: GeoJSON vs Arrow IPC vs FlatGeobuf vs Supabase+PostGIS, N ∈ 100–5000 sweep, `N_LOCK` decision rule (latency / egress / budget conditions). Expected crossover N 1000–2000; egress crossover ~6,700 loads/mo. Arrow JS has no IPC body compression (issue #109). File: `docs/performance/marker-format-cutoff-protocol.md`. RUN open.
- [ ] All 4 runs still open on locked laptop. Pass only with measured numbers; blank cell = gate open. Never backfill from `runtime-latency.md`.

Flag: `ocean-web/package.json` pins `cesium ^1.127.0` — d4 says pin `1.142.0` exact, not yet applied.

## D3 — ADRs (blocks M2/M4/M5) — all 5 written 2026-09-10

- [x] `docs/architecture/adr/ADR-001-pynio-drop.md` — Accepted. Drop PyNIO; xarray `engine=netcdf4` primary, `h5netcdf` fallback, `cfgrib` GRIB2. PyNIO NOT GitHub-archived but maintenance-mode Nov 2020 (README warning, tip `240ed46f` 2024-02-22); PyPI 1.0 no files; conda-forge last 1.5.5 Feb 2023, feedstock archived 2024-03-25. All free + live: netcdf4 1.7.4 MIT, h5netcdf 1.8.1 BSD-3, cfgrib 0.9.15.1 Apache-2.0, xarray Apache-2.0.
- [x] `docs/architecture/adr/ADR-002-cesium-over-three.md` (194 lines) — Accepted. Cesium `1.142.0` + zarr-cesium `0.2.0`. Reject: Three 0.186 (no WGS84/terrain/LOD/imagery/geo camera), three-globe 2.45 (sphere viz-only), netcdf-three (no npm, NetCDF v3-only), Babylon `@babylonjs/core@9.26.0` (same geo gap, no Zarr provider, WebGPU irrelevant issue #4989), Mapbox/MapLibre/deck (not globe engines), Google Photorealistic (proprietary + quota cliff). Free: CesiumJS Apache-2.0, zarr-cesium MIT, ion Community.
- [x] `docs/architecture/adr/ADR-003-opendap-facade.md` — Accepted. **Premise corrected: `xpublish-opendap` EXISTS (PyPI v0.2.0, BSD-3, pushed 2026-09-07), distinct from `xpublish-zarr` v0.1.0 (Apache-2.0).** Earlier "refuted/false" claim was wrong across comparison files — all corrected. Decision: MVP-skip facade, roadmap-only, hot path Zarr browser-direct, INCOIS interop via verified ERDDAP not unverified THREDDS. Free fallbacks: THREDDS/Unidata tds v5.9 (Unidata/thredds ARCHIVED 2022), Hyrax (pushed 2026-05-21), pydap 3.5.10 MIT; WMS/WCS GeoServer GPL / MapServer MIT. Never claim live compliance.
- [x] `docs/architecture/adr/ADR-004-chunking-strategy.md` — Proposed (pending benchmark). Default balanced `(1,10,540,1080)`; dual-rep `(30,50,108,216)` gated on chunk-bench G5∧G6∧G7; pyramid deferred. Cache folded in (`max-age=86400` + `lru_cache(1000)`, KTX2 3D-Tiles only). doi:10.1002/essoar.10511054.2 landing 403 → metadata via Crossref (title/authors match), marked UNVERIFIED.
- [x] `docs/architecture/adr/ADR-005-marker-format-cutoff.md` — Proposed (pending benchmark). Tiered N: `< N_LOCK` Supabase PostgREST bbox; `>= N_LOCK` R2 static (GeoJSON default, Arrow if parse >50 ms, FlatGeobuf if bbox-from-one-object). Cites marker-cutoff §7 N_LOCK. Arrow JS no IPC compression (#109).
- [x] `docs/architecture/adr/README.md` — index.

Two ADRs provisional until D2 measured runs. Still open: apply ADR-002 pin (`ocean-web/package.json` still `cesium ^1.127.0`).

## D4 — Phase 0 lib pins (blocks version ADR) — verified 2026-09-10, re-verified second pass

Method: exa + npm registry + GitHub API + raw + vendor docs curl. $NINEROUTER_KEY absent in tool shell, gateway skipped. Key never printed. Access date 2026-09-10 per URL.

- [x] zarr-cesium 0.2.0 latest triple-confirmed. Registry `https://registry.npmjs.org/zarr-cesium`: `dist-tags.latest 0.2.0`, `time.0.2.0 2026-08-21T14:32:01.298Z`, `modified 2026-08-21T14:32:01.660Z`. GitHub releases API: `v0.2.0 2026-08-21T14:32:02Z`. Tags API sha `1e5f18daccbb164ab3084edd8d2d09253a255b96` matches npm `gitHead`. Versions on npm: `0.1.0, 0.1.1, 0.1.3, 0.1.4, 0.1.6, 0.2.0`. Tag-only no-publish: `0.1.2, 0.1.5, 0.1.7`. npmjs page stale shows 0.1.4, releases page misses 0.2.0 — registry wins. Conflict note: velocity re-verify run saw registry latest 0.1.4 — stale read, rejected. Providers unchanged confirmed via `https://noc-oi.github.io/zarr-cesium/docs/` + raw `v0.2.0/README.md` + src list `zarr-layer-provider.ts, zarr-cube-provider.ts, zarr-cube-velocity-provider.ts`. url optional + store alt confirmed via raw `v0.2.0/src/types.ts` lines 39-42,71-74,98-101 + README lines 26-27,129-134 + release body Icechunk line. Peer exact `>=1.119.0 <2` confirmed via `https://registry.npmjs.org/zarr-cesium/0.2.0` + raw `v0.2.0/package.json`. Tested `cesium 1.142.0` confirmed via npm devDeps + raw package.json + `https://registry.npmjs.org/cesium/1.142.0`. Deps exact: `zarrita ^0.7.4`, `zarr-maps-tiling ^0.2.0`, `zarr-maps-colormap ^0.2.0`, NOC fork `cesium-wind-layer-0.11.1.tgz` GitHub release URL. zarrita latest now `0.7.5` published `2026-09-01T14:50:09.387Z` — `^0.7.4` allows it, combo untested, do not upgrade without test. Breaking 0.1.4→0.2.0: peer `*`→`>=1.119.0 <2`; deps 0→4 (needs GitHub tarball access); tiling/colormap migrated to Zarr Maps packages, types from `zarr-maps-tiling`, old deep imports break risk; velocity uses NOC fork; payload shrink `~12MB`→`314649`, files 29→6. Additive: store/stores, auth trio `requestOverrides/transformRequest/onAuthError`, queries `queryData/getTimeSeries/getVerticalProfile/getTransect`, GeoZarr multiscale, `CesiumHost = Viewer | CesiumWidget`. R2 public = plain url, needs CORS+Range confirmed via `https://developers.cloudflare.com/r2/buckets/cors/`, `.../public-buckets/` (r2.dev rate-limited, prod custom domain), `https://zarrita.dev/packages/storage.html`, `https://jsr.io/@zarrita/storage/doc`. R2-specific Range failure UNVERIFIED. FetchStore keys: `new FetchStore(url, {fetch, overrides deprecated, useSuffixRequest})`, transform must preserve Range via `new Request(newUrl, request)`. Pin: `zarr-cesium@0.2.0` + `cesium@1.142.0` + `zarrita@^0.7.4`, exact pre-1.0, no float.
- [ ] Phase 0 build retest still open. Steps: pin versions, verify `gitHead` starts `1e5f18d`; configure R2 public + CORS `GET,HEAD` + expose headers; probe chunk Range+Origin expect 206 + `Access-Control-Allow-Origin`, fix 401/403 first (not CORS); wire `url` public or `store` Icechunk/custom, `requestOverrides` static headers only, `transformRequest` sign/proxy, `onAuthError` refresh on 400/401; smoke `createLayer` + `queryData` Point, tiles 200/206, colormap update; typecheck `CesiumHost`, drop deep imports; inspect lockfile fork tarball + tiling/colormap 0.2.0. Fail → freeze 0.1.4 + cesium 1.119.0, log diff.
- [x] #13092 closed completed 2026-02-02T17:31:28Z by ggetz, fix PR #13098 merged 2026-02-02T17:31:03Z commit `7c3a70c`, author mzschwartz5. Release 1.138 published 2026-02-02T20:10:48Z, compare 1.137...1.138 contains fix then release commit. Strength: timeline strong, changelog-text weak (raw 1.138 CHANGES line 11 names only #13083, grep 13092|13098 zero on 1.138/1.139/1.142/1.145/main — link via PR body + close comment only). 1.136 broken (reporter env, adds pickAsync #12983 + terrain quadtrees #8481, bisect blames fast-picking #9961). 1.137 broken (CHANGES only #12949 #13042, predates merge). 1.138 fixed. 1.139/1.142/1.145 retained, no revert. 1.145 tag published 2026-09-01T18:51:41Z. Followup #13180 CV antimeridian not blocking. `enableCollisionDetection=false` invalid: default true, disables min/max zoom + tiles collision + CLAMP, 1.139.1 crash #13078 proof, reporter #12999 throttle laggy not viable. `frontFaceAlphaByDistance` valid fade API, not fix. Floor >=1.119 unsafe (includes 1.136–1.137). ADR min 1.138, prefer latest verified 1.145. 1.142 tie UNVERIFIED.
- [x] Velocity wrapper CONFIRMED cannot drop. Raw `main/src/zarr-cube-velocity-provider.ts` line 2 imports `WindLayer` runtime, class stores `WindLayer[]` per elevation slice, depth+time bound thru it. Fork latest v0.11.1 confirmed (`a1fcc4c`, v0.11.0 `6ec553c`, both 21 Aug, times 13:57 vs 11:58 match). Upstream 0.10.1 published 2026-04-26 peer `cesium ^1.127.0` dev `^1.136.0` confirmed. >=1.127 break REAL: engine 15 vs widgets-requires-16, dual ContextLimits singletons, error `Width must be <= maximum texture size (0)`. Fix VALID docs-only: Vite alias+dedupe + upgrade past 1.129 or pnpm overrides, PR #18 merged 2026-04-26 commit `80f6939`, released 0.10.1 via #19. Correction: Cesium 2019 wind blog `https://cesium.com/blog/2019/04/29/gpu-powered-wind/` LIVE 2026-09-10, prior dead claim false. Conflict: 0.2.0→v0.11.1 tarball pin UNVERIFIED this run (one agent saw npm latest 0.1.4 + libraries.io fork v0.11.0) — registry triple-confirm wins, pin maybe unpushed main. Fallback order UNVERIFIED, no local src (`grep` zero hits). Use: fork v0.11.1 primary, upstream 0.10.1 + alias second, custom shader last.
- [x] Vercel Hobby dual-sourced curl+exa. 100 GB Fast Data Transfer + 10 GB Fast Origin + 1M Edge Requests confirmed (`https://vercel.com/docs/limits` published 2026-09-03, `.../fair-use-guidelines`, `.../plans/hobby` published 2026-08-31, `.../pricing`, `.../manage-cdn-usage` published 2026-08-11). Number correct, label stale. Function invocations first 1M, Active CPU 4 CPU-hrs, Provisioned mem 360 GB-hrs. Builds Basic 2vCPU/8GB/32GB (`.../changelog/basic-build-machines` 2026-09-03), 45 min/deploy, 1 concurrent, 100/day, routes 2048, projects 200, domains 50. 6000 build bucket stale confirmed (`pricing` yaml `build_minutes hobby: not_available`, Pro `$0.0035/CPU/min`) — drop. Extra stale: `Static sites Unlimited`→200, `Custom domains 1`→50. Apr 2026 cut still UNVERIFIED. Real Apr changes: Turbo price cut 2026-04-15, Hobby 30-day retention 2026-04-27. Prior Pro credit-based 2025-09-09 + Flat Rate CDN 2026-09-08 not re-checked, UNVERIFIED this run. vercel.json valid (`.../project-configuration/vercel-json` published 2026-08-14, `.../routing/rewrites` published 2026-08-11, filesystem before rewrites, `/cesium/*` bypasses SPA). Caveat: `/cesium/(.*)` 1yr immutable needs `dist/cesium/` match — default `cesium()` plugin no custom assetsPath, dist UNVERIFIED without build. Patch: free-tier:78-80, vercel-deployment:21/279-285, cross-check:227 (see agent result for exact rows).
- [x] Ion Community dual-sourced. Storage 5 GB source-only tiled-excluded, Streaming 15 GB/mo ex-Bing, Global Imagery 1000 sessions/mo Bing+Google+Azure, Google Photorealistic 1000 root tiles/mo (= sessions per staff `https://community.cesium.com/t/cesium-ion-google-3d-tiles-api-limit/29307`), Geocodes 50000/mo widget-only no REST, Clips 10/mo 25 km2 Depot 200 km2 own, Reality 20 gigapixels / Analysis 5 hrs single-source. Sources: `.../platform/cesium-ion/pricing/`, `.../learn/ion/optimizing-quotas/`, `.../cesium-world-bathymetry/`, `.../sentinel-2-imagery/`, `.../ion-access-tokens/`, `.../rest-api/`. Bing burn per Viewer load confirmed + `Viewer.html` (`baseLayer:false` valid only if `baseLayerPicker:false`), `removeAll()` still burns, covering doubles. Fix: `baseLayer:false, baseLayerPicker:false` + Sentinel-2 assetId 3954 (10m global, data-quota cached refresh). Token: header `Bearer`, dashboard never expires vs OAuth monthly (`.../ion-oauth2/`) — different types. Bathymetry = GEBCO 2023 Grid doi:10.5285/f98b053b-0cbc-6c23-e053-6c86abc0af7b + high-res to 1m, SaaS Depot + Self-Hosted licensable. Sufficiency CONDITIONAL PASS: Sentinel-2 + CWB only, uploads <5GB, streams <15GB, geocodes minimal, clips ≤10 — actual demo bytes/sessions absent, measure via Usage Dashboard. Over-quota soft (email first), then Commercial 50GB/150GB/5k/5k/25 or sales. Reduce: Sentinel-2, `geocoder:false`, WebP, higher SSE, lower resolutionScale.

## D4-best — best-of-best free picks (5 agents, 2026-09-10)

- Render: custom ImageryProvider + zarrita FetchStore + Canvas + d3 wins. Smallest bundle, deps present, full depth+time. zarr-cesium B-arm only. netcdf-three dropped (Three-only, stale 2024, v3-only, no npm). FPS/chunk-ms missing all paths — Phase 0 A/B bench owed. `ZarrImageryLayer` exists in 0.2.0 exports (prior absent claim corrected). Standalone npm `cesium-zarr`/`ZarrTileProvider`/`ZarrImageryLayer` 404.
- Pin: `cesium@1.142.0` exact wins. 101d soak beats 9d 1.145 churn. Avoids 1.145 ClippingPolygons freeze + 1.119 age/upstream peer fail. #13092 fix UNVERIFIED in CHANGES either way — laptop matrix test owed. ADR text ready (never float latest). Fallback `1.138.0` exact (zero breaking, smallest bundle). Sizes: 1.119 68.7MB, 1.138 74.7MB, 1.142 77.8MB, 1.145 79.4MB (registry single-source).
- Particles: fork v0.11.1 via provider primary. Upstream 0.10.1 + alias second. Vendored shader last. RaymanNg dropped (NetCDF v3 only, single timestep, pins cesium 1.125.0, MIT 155 commits latest 2025-06-18).
- Host: Cloudflare Pages wins. Unlimited BW+requests, 500 builds/mo. Bind: 25 MiB/file → big Cesium chunks in R2. Vercel Hobby non-commercial ban. Netlify ~15 GB effective. GH Pages no headers/rewrite + commercial ban. Equivalents ready: `_headers`+`_redirects`, `netlify.toml`, vercel.json keep. `dist` never built — largest-file UNVERIFIED, build once then check.
- Base: GIBS Blue Marble + ion World Bathymetry wins. Keeps $0, adds true depth. Current code = option #2 (World Terrain land-only) — swap one call `Terrain.fromWorldBathymetry({requestVertexNormals:true})`. Storage correction: live page 10 GB not 5 GB. Bathymetry assetId 2426648, GEBCO 2023, ~450m + hi-res coasts. Google tiles dropped (quota cliff + oceans weak + bans). Drop: Bing base, Geocoder, night-lights call, auto-tests on ion.
- Still open: locked-laptop matrix (pin × translucency × ion × wind × zarr-cesium), terrain bytes/load, quota headroom 20 reloads, offline reload, deploy A/B Pages+Vercel preview.

## D5 — Unverified externals (cite/drop) — superseded by §D5-best below

## D5-best — externals resolved + gods-eye-view (5 agents, 2026-09-10)

- Esri target resolved. #1 EMU Explorer cite — `https://livingatlas.arcgis.com/emu/`, JSAPI 4.24 SceneView+WebScene, code Apache-2.0, app modified 2026-02-19. Primary 3D ocean baseline + UX pattern. #2 Ocean basemap cite base-only — `.../Ocean/World_Ocean_Base/MapServer`, live 17 LODs scales 591657527→9027, modified 2026-05-26, attribution verbatim `Esri, Garmin, GEBCO, NOAA NGDC, and other contributors`, GEBCO no-navigation note. #3 Ocean Voxels drop as dependency (code repo 404, license `For demo purposes only`) cite voxel prior art only. #4 Living Atlas generic drop as target. Copy keys: `esri/views/SceneView`, `esri/WebScene`, `view.goTo({tilt:45})`, `basemap:"sat"`, VoxelLayer docs URL, both tile URLs + Reference, tileInfo JPEG/3857/0-16, EMU 37 units + 6 vars, item IDs `58526e3af88b46a3a1d1eb1738230ee3`/`1e126e7520f9466c9ca28b8f28b5e500`/`a1ba51fed98144de83bb2cc287550e20`/`6a16bb85e9ab47829aa626dced51edc6`. Cesium instead: `camera.flyTo` pitch presets in repo JSON, pre-sliced volumes + clippingPlanes + profile panel, two imagery layers with attribution widget. Best: EMU cite + Ocean Base underneath + Sayre 2017 + USGS DOI `https://doi.org/10.5066/P9Q6ZSGN`. Lazier: tiles + EMU summary sheets, skip voxel build.
- INCOIS resolved, prior fail fixed. Holdings page 200 44 rows. ERDDAP live 17 datasets (griddap `.nc` + tabledap `.csv` + WMS): `.../erddap/info/index.csv`, per-dataset `.das/.dds/.csv/.nc/.graph`, FGDC+ISO19115, RSS. LAS JSON live: `getCategories.do` 13 cats, `getDatasets.do?catid=` works (SST cat ~17MB latin-1). ESSDP 200 16 metadata links, 1047 datasets claim. Copyable curl: griddap NOAA SST subset (time/zlev/lat/lon axes, returns rows e.g. 27.31), tabledap `Indian_ARGO_Floats.csv` platform/lat/lon/time, LAS cats+datasets. Ingest order: ERDDAP first, LAS second, holdings static ref only. THREDDS timeout UNVERIFIED do not code. Cert needs `-k` (missing intermediate). Verdict cite-with-links. Card fields ready for `incois-las.md` (44 rows, formats, auth split, ERDDAP+LAS subset method; sha/license/bbox still empty). NOAA SST regional only (lat -29.875–29.875, lon 20.125–139.875, time ends 2011-10-04).
- Mapbox verified dual-sourced. Globe enum + `setProjection/getProjection`, `flyTo/easeTo/jumpTo` full signatures (`curve 1.42`, `speed 1.2`, `essential:true`, `preloadOnly`, `fov`, `around`), terrain keys (`raster-dem`, exaggeration 0-1000, globe|mercator, SDK ≥2.0), fog keys (color/high-color/horizon-blend/range/space/star/vertical-range + version gates ≥2.3/2.9/3.0). Model verified (per Map Load, 12h session). $ bands provisional only, re-fetch pricing before external cite. Cesium equivalents: `flyTo`+`flyToBoundingSphere`+`flyHome`, `EasingFunction.*`, `TerrainProvider.fromIonAssetId`, `screenSpaceCameraController`. Dropped: `CameraFlightPath` 404, `setView` + `FreeCameraOptions` unverified.
- gods-eye-view: useful YES as reference architecture, NOT fork/dependency. `bilawalsidhu/gods-eye-view`, CesiumJS-only `cesium ^1.124.0` vanilla JS + vite 6 + satellite.js, no Three/MapLibre/deck/zarr/netcdf/particles. Globe yes (Google tiles direct+ion, Esri keyless, OSM, Re:Earth terrain). Fly-to yes (presets, POIs, search). Particles no (traffic dots = PointPrimitives lerp). Markers yes (5000+ billboards, PointPrimitiveCollection, CLAMP ellipses). Profiles no. Time partial (live poll + SGP4, no slider). Data JSON APIs via Vite `/api/*` proxies + governors (OpenSky TTL+429+stale, TomTom 40000/day, `.gev-cache/`). License code MIT with third-party carve-out (GitHub API NOASSERTION): TeleGeography CC BY-NC-SA delete for commercial/gov, OSM/Overpass/adsb.lol ODbL share-alike, OpenSky non-commercial, TomTom/Google BYOK. Counts dated 2026-09-10: stars 22311, forks 4744, issues 148, pushed 2026-09-05 (Exa 541 stale rejected). Copy rank: `renderGovernor.js` idle-GPU fix #1, `data/manager.js` feed states #2, `sharelink.js` URL hash #3, `mapStackController.js`+`mapStartup.js` Esri/Re:Earth fallbacks #4, earthquakes+localGeojson ellipse perf 32.4→1.4ms #5, satellites SGP4 #6, trailRenderer depth-fail alpha #7, flights billboard batching #8 (262KB do not port), camera presets #9, vite proxy governors #10 (extract per-route), dataCredits #11, director cinematic #12 demo-video only. Implement: governor holds in `config.ts`, TS-lite layer manager, extend `base-imagery.ts` Esri+Re:Earth, hash share links in viewer/App, billboard+trail vessel/Argo layers, server proxies in ocean-api. Drop: voice/OpenAI, CCTV, radio, bikeshare, traffic sim, cockpit, detection overlays, TeleGeography bundle.
- NASA Eyes: engine undisclosed, inspiration-only. Copy: object-as-query click→fly+panel + Home, orbit/zoom/double-click + ride-along + Draw Mission, LIVE + 1950-2050 scrub + speed (Cesium timeline native suffices), layer toggles + type filters, full-DB search + curated lists + Browse, next-5 watchlist + countdown, Vital Signs animate, Learn scrolly. 10-step M5 tour in agent result.
- D5 boxes: Esri cite, INCOIS cite-with-links, Mapbox verified, Eyes rule locked. CLOSED 2026-09-10:
  - `docs/data/incois-las.md` written (10.8K): 16 ERDDAP IDs, 13 LAS cats, 44 holdings rows, 5 key `.das` bbox/time, license verbatim, sha NONE explicit (live APIs), 8 assumptions. THREDDS single DatasetScan no per-dataset listing. ESSDP 1047 string NOT found — claim UNVERIFIED.
  - USGS EMU dual-sourced: DOI 10.5066/P9Q6ZSGN → ScienceBase `642c9bd0d34ee8d4add254c0`; 37 units, 6 vars exact, 4 bands with meters, 52M+ points, 57-yr WOA13 v2, 22/15 split. Item `58526e3a...` CC BY 4.0 item-only. Repo Apache-2.0. UNVERIFIED: interval breakdown 5/25/50/100m, mesh 1° nuance, deed text (redirect). Cite-ready block in agent result.
  - Mapbox pricing official: Map Loads 50k free then $5/$4/$3/$2.50 (adds $2.50 tier 1M-5M — prior provisional corrected); Directions 100k then $2/$1.60/$1.20; Geocoding 100k then $0.75/$0.60/$0.45; Static 50k then $1/$0.80/$0.60; MAU 25k then $4/$3.20/$2.40; session 12h max. PROVISIONAL dropped.

## D6 — Capacity math (blocks M2/M4) — superseded by §D6-best below

## D6-best — capacity precise (3 agents, 2026-09-10)

- R2: bbox 60-100E/5S-30N = 480×420 = 201,600 cells/slice = 0.806 MB float32. Per stored month @10:1: monthly 1-step surface 0.40 MB / 5-depths 1.69 MB / full-50 16.21 MB; daily ~30.44 steps surface 12.27 MB / 5-depths 51.55 MB / full-50 493.39 MB. Fit 10,000 MB: daily full-50 20.3 mo (~1.7 yr), daily 5-depths 194 mo, daily surface 815 mo. ×1.074 if GiB counting. Ratio caveat: 10:1 optimistic ceiling — measured lossless ocean float32 1.35-3x (CESM zstd 1.35x, earth cube Blosc ~3.06x). At 3x daily full-50 = ~6 mo. Chunk rule: one chunk per var-level per month (time=30), daily-per-step explodes Class A (free 1M A/mo covers bulk ingest only coarse). Isosurface cache ~1-10 MB/mesh UNVERIFIED — keep local/IndexedDB LRU, out of R2. Default: daily surface + daily 5-depths rolling 12 mo (~766 MB) + monthly full-50 24 mo (~389 MB) = ~1.2 GB. Full-50 daily fits ~20 mo zero headroom — avoid default. Fallback: drop uo/vo → cut months → coarser chunks → float16 unverified. Broader IO 40-120E/30S-30N = ×3.43 cells (arithmetic unverified).
- Supabase: 4000×100 claim FALSE — 39.2M rows × 76B = ~3 GB heap, 6x over before indexes. PG row math MAXALIGN 8 + 24B header: floats row ~84B, profiles row ~76B. Full IO history 700-900 floats × 100 cycles = 527-678 MB heap NO FIT. Latest-10 + metadata ~60 MB fits. Metadata-only 7 MB. Overhead unverified: btree +130-260 MB, GiST +3-5 MB, WAL pushes disk > DB size. Plain lat/lon + btree beats PostGIS (saves ~20 MB). R-tree SQLite-only do not port. JSONB worse, normalized wins. Pause: 7d inactivity, warning email, dashboard resume, 1-yr restore, 2 projects, 500 MB then read-only, 5 GB egress. Keep-warm = daily real queries (SELECT 1 sufficiency unverified), alert on email, monitor `pg_database_size`, fallback at 400 MB / 5M rows / 4 GB egress → strict subset (metadata + latest 10, ~60 MB). R2 JSON fallback wins full history: `r2://argo-indian/{wmo}/{cycle}.json` + manifest, egress-free. Lazier: skip Supabase, R2 JSON + client index.
- Chunk+cache winner: single balanced `(1,10,540,1080)` + `max-age=86400` + `lru_cache(1000)` + no pyramid + KTX2 only 3D Tiles. Holds free tier, smallest cold (~2.33 MB/chunk). Dual-rep runner-up only if time-animation dominant (~2x GB). Slice chunk ~2.33 MB slightly over 100KB-2MB sweet spot, time chunk ~14 MB far over. 40x gain UNVERIFIED (corroboration 713-1405x worst-case tradeoff). Pyramid ×1.14 3D / ×1.33 2D. Repeat-view 0 Class B second load (hit rate workload-dependent). lru 50 vs 500 ms per doc, precompute-vs-lru unverified. Benchmark steps 1-7 in agent result (both chunkings, cold slice, cold start throttled, repeat 304 count, isosurface 10-run, pyramid GB mult, free-tier sum vs 10GB/1M/10M).
- D6 boxes: R2 default scope locked (~1.2 GB), Supabase latest-10 + metadata (~60 MB) + R2 JSON history, chunk single balanced default. Still open: locked-laptop runs (slice ≤750 ms, cold ≤10 s, 30 FPS + 5000 markers, Arrow cutoff, chunk benchmark steps).

## D7 — Mandate beats (blocks M5 pitch) — superseded by §D7-best below

## D7-best — mandate beats (4 agents, 2026-09-10)

- Hazard winner: cyclone/storm-surge Fani 2019 Bay arc. Datasets: `ascat_daily_datasets` (wind U/V 10m) + `incois_valueadded_products_datasets` (D26/HTCNT/MLD/GEO_U/GEO_V) + `incois_argo_10d_VAM` (TEMP/SAL 5-2000m) + `cmems_mod_glo_phy_my_0.083deg_P1D-m` (thetao/uo/vo/zos/mlotst) + `Indian_ARGO_Floats` markers. Tsunami dropped (viz-only no download). Static SST dropped (ends 2011-10-04). Preset lon 80-95 lat 5-22, depths [5,25,50,100,150,200,300,500], steps 2019-03-30/04-27/05-02/05-03. ERDDAP 1-pt CSV no-auth OK (ASCAT 1.7 m/s, VAP D26 60.24m). 90s script in agent result. GLORYS free + registration.
- SAR winner: drift Arabian Sea Konkan. Datasets: GLORYS DOI `10.48670/moi-00021` uo/vo surface + GDP `drifter_6h` CC0 (ve/vn) + hourly QC + SARAT context + HF radar registered overlay-only. Preset bbox 68-74E/12-20N outer 60-100E/-5-30N, depth [0.49] surface only, 5 daily steps. NIOT page timeout UNVERIFIED (Exa snapshot only). 12-line 90s script in result. Free: Copernicus account + NOAA public domain.
- Fishery winner: SST front + chlorophyll PFZ logic. Datasets: `NOAA_AVHRR_AMSR_datasets` sst/anom 3413 steps surface-only + `IRS_chlorophyll_datasets` 97 steps surface-only + PFZ holdings ASCII/JPEG (access blank → restricted-by-default). Joint window lon 60-104 lat 0-25, overlap 2003-01-05–2006-03-21, demo 2005-01-15. Thresholds PFZ paper: 0.3°C 3×3 + 0.5 mg/m³ 5×5. Curtain = legibility wall 0 to -300m not data. ERDDAP `.nc` URLs exact in result. IRS cadence UNVERIFIED. PFZ page live: 586 centers, 14 sectors.
- Climate winner: basin warming vs baseline. Datasets: GLORYS P1M-m + climatology baseline + NOAA OISST `ncdcOisst21Agg` DOI `10.25921/RE9P-PT57` + Argo DOI `10.17882/42182`. Preset lon 60-100 lat -5-30, 0-2000m 50 levels, 12 monthly 2026. QC flag 1 only, nearest-cell collocate. Residual canonical never flipped. NOAA pages JS-gated Exa-only. 90s script in result.
- D7 boxes: 4 beat cards + presets + scripts + dataset IDs done. Still open: pitch doc assembly, live subset fetch wiring, PFZ text scrape, HF registered pull.

## D8 — Mermaid sign + open-question gates (blocks M4/M5) — sign DONE, gates open

- [x] Residual sign: all 3 mermaid files now canonical `E = M − O` (observed working tree 2026-09-10, mtimes 18:55; attribution unknown, content verified vs `PROBLEM-STATEMENT-ANALYSIS.md:70`). Files: `research/individuals/mermaid/06-scientific-analytics/analytics.md`, `04-interaction-patterns/pattern-library.md`, `deep-dive-analysis.md`.
- [x] Panel check: zero `O − M` in code — no residual/compare panel exists (`ocean-web/src/` globe-only). Re-grep before M4 ships a panel.
- [ ] Q7 gate: one-feature proof (eddy/front) + independent cross-check + false-positive rate before any feature claim. OPEN.
- [ ] Q9 gate: GPU-vs-server benchmark on locked device before committing GPU analytics. OPEN.
- [ ] Q10 gate: random-access bounded-volume proof (bbox + 3-5 depths + 3-5 steps + 1 var) before volume claims. OPEN.
- Detail: `docs/research/d8-sign-and-gates.md`.

## Order

D4 Phase 0 test first (determines render path). D1 cards parallel. D2+D6 measured runs next. D3 ADRs after measurements. D5 cite/drop anytime. D7 pitch anytime before M5. D8 sign rewrite before any M4 panel work.
